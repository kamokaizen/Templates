global.conf = require('../config/config.test.json');

var assert = require('assert');
var sinon = require('sinon');
var log4js = require('log4js');
var log = log4js.getLogger("map-worker-test");

var mapWorker = require('../workers/map_worker');

// TODO complete the test case implementation
describe('Map worker Functional Tests', function () {

    before(function (done) {
        // runs before all tests in this block
    });

    after(function () {
        // runs after all tests in this block
    });

    beforeEach(function () {
        // runs before each test in this block
    });

    afterEach(function () {
        // runs after each test in this block
    });

    // test cases
    it('should initialize map', function (done) {
        done();
    });

    it('should get map', function (done) {
        done();
    });

    it('should insert value', function (done) {
        done();
    });

    it('should get value', function (done) {
        done();
    });

    it('should delete value', function (done) {
        done();
    });

    it('should delete value', function (done) {
        done();
    });
});